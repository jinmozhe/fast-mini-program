# API配置
API_V1_STR=/api/v1
PROJECT_NAME=外卖配送平台API
BACKEND_CORS_ORIGINS=["http://localhost", "http://localhost:4200", "http://localhost:3000"]
ENVIRONMENT=test

# JWT配置
SECRET_KEY=test_secret_key_do_not_use_in_production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# 数据库配置
POSTGRES_SERVER=localhost
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=delivery_app_test
SQLALCHEMY_DATABASE_URI=postgresql+asyncpg://postgres:postgres@localhost/delivery_app_test

# 日志配置
LOG_LEVEL=DEBUG
LOG_FORMAT=text

# 文件上传配置
UPLOAD_DIR=uploads_test
MAX_UPLOAD_SIZE=5242880  # 5MB
ALLOWED_EXTENSIONS=["jpg", "jpeg", "png", "pdf", "doc", "docx"]

# 短信服务配置
SMS_PROVIDER=mock
SMS_ACCESS_KEY=test_key
SMS_SECRET_KEY=test_secret
SMS_REGION=cn-hangzhou
SMS_SIGN_NAME=测试签名
SMS_TEMPLATE_CODE=SMS_TEST_CODE

# 电子邮件配置
EMAILS_ENABLED=False
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=test
SMTP_PASSWORD=test